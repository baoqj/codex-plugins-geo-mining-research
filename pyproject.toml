[project]
name = "geo-mining-research"
version = "0.1.0"
description = "GeoMine Research Codex plugin helper package."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["scripts"]
addopts = "-q"
