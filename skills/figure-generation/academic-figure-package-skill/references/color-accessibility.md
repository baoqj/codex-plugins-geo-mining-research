# Color Accessibility

- Do not rely on red/green-only encoding.
- Use redundant encodings: symbol shape, line style, labels, hatching, or direct annotations.
- Keep sufficient contrast between text and background.
- Use colorblind-aware palettes for categorical data.
- Use sequential palettes for ordered values and diverging palettes only when there is a meaningful center.
- Test grayscale readability for print.
