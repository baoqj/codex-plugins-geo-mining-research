## PHREEQC Methods

PHREEQC calculations were designed to evaluate <research_objective>. The model type was classified as <model_type> because <classification_reason>. Input water chemistry was derived from <data_source> and audited for sample identifiers, units, pH, temperature, redox handling, alkalinity basis, major ions, trace elements, and QA/QC constraints.

The calculations used <database_name> (<database_path>) because <database_selection_reason>. The input file included <keyword_list>. Missing data were retained as explicit placeholders (<placeholder_list>) and were not estimated. Selected outputs were configured to report <selected_output_list>.

The model was run with:

```bash
phreeqc <input_file> <output_file> <database_file>
```

Calculated values were interpreted as <interpretation_scope>. The model does not by itself validate field-scale boundary conditions, kinetic parameters, surface complexation constants, exchange capacities, mineral amounts, or regulatory conclusions.
